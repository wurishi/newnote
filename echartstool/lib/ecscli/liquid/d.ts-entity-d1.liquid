  /** Entity Extensions for {{ namespace }} */
        {% for c in c1 %}static {{ c.name }}: {{ c.Name }}Component;
        is{{c.Name}}: boolean;
        set{{c.Name}}(value: boolean): Entity;
        {% endfor %}
        {% for c in c2 %}static _{{ c.name }}ComponentPool: Bag<{{ c.Name }}Component>;
        static clear{{ c.Name }}ComponentPool(): void;
        {{ c.name }}: {{ c.Name }}Component;
        has{{ c.Name }}: boolean;
        add{{ c.Name }}({{ c.p }}): Entity;
        replace{{ c.Name }}({{ c.p }}): Entity;
        remove{{ c.Name }}(): Entity;
        {% endfor %}